name: Build and Deploy to ECR

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  id-token: write
  contents: read

env:
  AWS_REGION: us-east-1
  ECR_REPOSITORY: ${{ vars.ECR_REPOSITORY }}
  CONTAINER_NAME: ${{ vars.CONTAINER_NAME }}
  API_PORT: ${{ vars.API_PORT }}
  WEB_PORT: ${{ vars.WEB_PORT }}
  SERVICE_NAME: ${{ vars.SERVICE_NAME }}
  SERVICE_SLUG: ${{ vars.SERVICE_SLUG }}

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    # Only run if ECR_REPOSITORY is configured (skip for template repo itself)
    if: ${{ vars.ECR_REPOSITORY != '' }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install API dependencies
        run: npm ci

      - name: Install Web dependencies
        run: cd web && npm ci

      - name: TypeScript check (API)
        run: npm run typecheck

      - name: Build API
        run: npm run build

      - name: Build Web
        run: cd web && npm run build
        env:
          SERVICE_NAME: ${{ vars.SERVICE_NAME }}
          SERVICE_SLUG: ${{ vars.SERVICE_SLUG }}
          BASE_PATH: /${{ vars.SERVICE_SLUG }}

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: arn:aws:iam::743143972162:role/github-actions-ecr-role
          aws-region: ${{ env.AWS_REGION }}

      - name: Login to Amazon ECR
        id: login-ecr
        uses: aws-actions/amazon-ecr-login@v2

      - name: Build and push Docker image
        env:
          ECR_REGISTRY: ${{ steps.login-ecr.outputs.registry }}
          IMAGE_TAG: ${{ github.sha }}
        run: |
          docker build \
            --build-arg SERVICE_NAME="${{ vars.SERVICE_NAME }}" \
            --build-arg SERVICE_SLUG="${{ vars.SERVICE_SLUG }}" \
            --build-arg BASE_PATH="/${{ vars.SERVICE_SLUG }}" \
            -t $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG \
            -t $ECR_REGISTRY/$ECR_REPOSITORY:latest .
          docker push $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG
          docker push $ECR_REGISTRY/$ECR_REPOSITORY:latest

      - name: Deploy to EC2
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: 52.203.207.103
          username: ec2-user
          key: ${{ secrets.EC2_SSH_KEY }}
          script: |
            # Login to ECR
            aws ecr get-login-password --region us-east-1 | docker login --username AWS --password-stdin 743143972162.dkr.ecr.us-east-1.amazonaws.com

            # Pull latest image
            docker pull 743143972162.dkr.ecr.us-east-1.amazonaws.com/${{ env.ECR_REPOSITORY }}:latest

            # Stop and remove existing container
            docker stop ${{ env.CONTAINER_NAME }} 2>/dev/null || true
            docker rm ${{ env.CONTAINER_NAME }} 2>/dev/null || true

            # Run new container with both API and Web ports
            docker run -d \
              --name ${{ env.CONTAINER_NAME }} \
              --network kohlex-network \
              -p ${{ env.WEB_PORT }}:3000 \
              -p ${{ env.API_PORT }}:3001 \
              -e PORT=3001 \
              -e WEB_PORT=3000 \
              -e SERVICE_NAME="${{ vars.SERVICE_NAME }}" \
              -e SERVICE_SLUG="${{ vars.SERVICE_SLUG }}" \
              -e NODE_ENV=production \
              --restart unless-stopped \
              743143972162.dkr.ecr.us-east-1.amazonaws.com/${{ env.ECR_REPOSITORY }}:latest

            # Health check
            sleep 10
            curl -f http://localhost:${{ env.API_PORT }}/health || exit 1

      - name: Cleanup old ECR images
        run: |
          IMAGES=$(aws ecr describe-images --repository-name $ECR_REPOSITORY --query 'imageDetails[?imagePushedAt<`'$(date -d '7 days ago' -Iseconds)'`].imageDigest' --output text 2>/dev/null || true)
          if [ -n "$IMAGES" ]; then
            for digest in $IMAGES; do
              aws ecr batch-delete-image --repository-name $ECR_REPOSITORY --image-ids imageDigest=$digest || true
            done
          fi
